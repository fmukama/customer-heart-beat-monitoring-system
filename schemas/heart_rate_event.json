{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "heart-rate-event",
  "title": "Heart Rate Event",
  "description": "A single heart-rate reading generated by a simulated customer sensor.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "event_id",
    "customer_id",
    "heart_rate",
    "event_time"
  ],
  "properties": {
    "event_id": {
      "type": "string",
      "format": "uuid",
      "description": "Globally unique identifier for this event."
    },
    "customer_id": {
      "type": "string",
      "minLength": 1,
      "description": "Identifier of the simulated customer."
    },
    "heart_rate": {
      "type": "integer",
      "minimum": 20,
      "maximum": 250,
      "description": "Heart rate in beats per minute."
    },
    "event_time": {
      "type": "string",
      "format": "date-time",
      "description": "UTC timestamp representing when the measurement occurred."
    }
  }
}